---

 - name: Lock mongodb-server package version
   lineinfile: dest=/etc/yum.repos.d/mongodb.repo
               regexp="exclude=mongo-10gen-server"
               line="exclude=mongo-10gen-server"
               state=present
   tags:
     - mongodb_server_package_locker
